securitySchemes:
  bearerAuth:
    type: http
    scheme: bearer
    bearerFormat: JWT

tags:
  - Admins
security:
  - bearerAuth: []
responses:
  200:
    description: Returns system statistics
  401:
    description: Unauthorized access
